<a href="#kontakt" class="sticky-cta" aria-label="Termin vereinbaren">
  <img src="/phone-line.svg" width="30" height="30" alt="" />
</a>

<style>
  .sticky-cta {
    display: none;
    position: fixed;
    bottom: 24px;
    left: 24px;
    background-color: var(--color--primary);
    width: 56px;
    height: 56px;
    border-radius: 50%;
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.4);
    z-index: 100;
    transition: transform 0.2s;
    justify-content: center;
    align-items: center;
  }

  .sticky-cta img {
    filter: brightness(0) invert(1);
    width: 28px;
    height: 28px;
  }

  .sticky-cta:hover {
    transform: scale(1.05);
  }

  @media (max-width: 768px) {
    .sticky-cta {
      display: flex;
    }
  }
</style>
