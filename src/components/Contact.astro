---
// Appointment section component extracted from index.astro
---

<section class="appointment-section section-spacing-bottom container">
  <div class="flex-center">
    <div class="appointment-section-header">
      <h1 class="appointment-section-title">Kontakt & Öffnungszeiten</h1>
      <a href="tel:+41712222022" class="button-primary-2 call-now-btn"
        >Jetzt anrufen</a
      >
    </div>
    <div class="appointment-info-box">
      <img src="info.svg" alt="Info" class="appointment-info-icon" />
      <div>
        Termine ausschliesslich nach telefonischer Vereinbarung über Beauty
        Cosmetics.
      </div>
    </div>
  </div>
  <div class="appointment-table-grid">
    <div class="appointment-table">
      <h2 class="appointment-table-title">
        Mögliche Botulinumtoxin-Termine mit <span style="white-space: nowrap;"
          >Dr. Scheffbuch</span
        >
      </h2>
      <table class="appointment-hours-table">
        <tbody>
          <tr><td>Mittwoch</td><td>17:00 – 19:00</td></tr>
          <tr><td>Samstag</td><td>10:00 – 15:00</td></tr>
        </tbody>
      </table>
    </div>
    <div class="appointment-table">
      <h2 class="appointment-table-title">Beauty Cosmetics Anrufzeiten</h2>
      <table class="appointment-hours-table">
        <tbody>
          <tr><td>Montag</td><td>08:30 – 19:00</td></tr>
          <tr><td>Dienstag</td><td>08:30 – 19:00</td></tr>
          <tr><td>Mittwoch</td><td>08:30 – 19:00</td></tr>
          <tr><td>Donnerstag</td><td>08:30 – 19:00</td></tr>
          <tr><td>Freitag</td><td>08:30 – 19:00</td></tr>
          <tr><td>Samstag</td><td>08:30 – 16:00</td></tr>
          <tr><td>Sonntag</td><td>Geschlossen</td></tr>
        </tbody>
      </table>
    </div>
  </div>
  <!-- Test -->
</section>
<style>
  .flex-center {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }
  .appointment-section-header {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
    flex-wrap: wrap;
  }
  .appointment-section-title {
    font-size: 2.2rem;
    font-weight: 700;
    color: var(--color--primary-1);
    text-align: left;
    margin-bottom: 0;
  }
  .appointment-info-box {
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    width: fit-content;
    gap: 0.7rem;
    background: #e5ecf9;
    border-left: 4px solid var(--color--primary-2);
    padding: 0.9rem 1.2rem;
    border-radius: 0.5rem;
    font-size: 1.05rem;
    color: var(--color--primary-1);
    margin-bottom: 2.2rem;
    margin-top: 0;
  }
  .appointment-info-icon {
    color: var(--color--primary-2);
    flex-shrink: 0;
  }
  .call-now-btn {
    font-size: 1rem;
    width: fit-content;
    padding: 0.7rem 1.5rem;
    border-radius: 2rem;
    background: var(--color--primary-2);
    color: #fff;
    border: none;
    text-decoration: none;
    font-weight: 600;
    transition: background 0.2s;
    box-shadow: 0 2px 8px 0 rgba(9, 19, 49, 0.04);
    display: inline-block;
    margin-bottom: 0;
  }
  .call-now-btn:hover {
    background: var(--color--primary-1);
    color: #fff;
  }
  .appointment-table-grid {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 2rem;
  }
  .appointment-table {
    border-radius: 1rem;
    box-shadow: 0 2px 8px 0 rgba(9, 19, 49, 0.04);
    padding: 2rem 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    background-image: linear-gradient(
      40deg,
      var(--color--secondary-3) 11%,
      #e5ecf91a 50%,
      var(--color--secondary-3) 87%
    );
    width: 400px;
  }
  .appointment-table-bg {
  }
  .appointment-table-title {
    font-size: 1.3rem;
    font-weight: 600;
    color: var(--color--primary-2);
    margin-bottom: 1.2rem;
  }
  .appointment-hours-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 1.1rem;
    color: var(--color--primary-1);
  }
  .appointment-hours-table td {
    padding: 0.5rem 0.7rem;
    border-bottom: 1px solid #e5ecf9;
  }
  .appointment-hours-table tr:last-child td {
    border-bottom: none;
  }
  @media (max-width: 900px) {
    .appointment-table-grid {
      grid-template-columns: 1fr;
    }
    .appointment-table {
      min-width: unset;
    }
  }
  @media (max-width: 700px) {
    .appointment-section-header {
      flex-direction: column;
      gap: 0.7rem;
    }
    .appointment-section-title {
      font-size: 1.3rem;
    }
    .call-now-btn {
      text-align: center;
    }
  }
</style>
